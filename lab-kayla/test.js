'use strict';

const expect = require('chai').expect;
const fs = require('fs');

describe('lib', function() {
  describe('#readFile', function() {
    it('should read files and produce hex')
    let one = `${__dirname}/dataFile/dataOne.txt`;
    let two = `${__dirname}/dataFile/dataTwo.txt`;
    let tree = `${__dirname}/dataFile/dataThree.txt`;
  });
});
